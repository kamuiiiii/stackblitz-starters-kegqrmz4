{
  "name": "node-starter",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "INITIAL_CALL='10! = ?' SKIP_LOOP=true bun index.ts",
    "start:code-execution": "INITIAL_CALL='10! = ?' SKIP_LOOP=true bun code-execution.ts"
  },
  "dependencies": {
    "@aigne/cli": "^1.8.1",
    "@aigne/core": "^1.13.0",
    "typescript": "^5.8.3",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@types/node": "^22.9.0"
  }
}
